import styles from './core/index.scss'

/**
 * Resolves the defined assets within the actual module context. This file
 * is only used for the builder reference and does not need to be included
 * within the compiled library.
 */
export default () => ({ styles })
